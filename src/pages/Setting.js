import React from "react";
import Button from "../components/common/Button";

const Setting = () => {
  return (
    <div>
      설정
      <Button>버튼</Button>
    </div>
  );
};

export default Setting;
